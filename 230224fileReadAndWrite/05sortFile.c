#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
 * 打开文件
 * 把数据存到buf
 * 排序
 * 输出
 * */
int main()
{
	FLIE *fp = fopen("./sortData.txt","r");
	char buf[1024];
	int bufLen = 1024;


	return 0;
}
